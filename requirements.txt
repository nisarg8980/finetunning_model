# Tested together around mid-2025. Adjust if your CUDA/driver differs.
# Install torch matching your CUDA build first: https://pytorch.org/get-started/locally/
torch>=2.3.0
transformers>=4.44.0
trl>=0.12.0
peft>=0.12.0
datasets>=2.20.0
accelerate>=0.33.0
bitsandbytes>=0.43.0   # 4-bit QLoRA + paged 8-bit optimizer (NVIDIA GPU required)
sentencepiece>=0.2.0
scipy>=1.11.0
